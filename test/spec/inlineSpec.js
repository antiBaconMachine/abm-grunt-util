var inliner = require('../index').inline;
var MockGrunt = require('./mocks/mockGrunt');

describe('Inline task processing', function() {
   it('should extend grunt config with tasks and return string identifiers of those tasks', function() {

   });
});